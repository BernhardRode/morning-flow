{
  "name": "morning-flow",
  "version": "1.0.0",
  "private": true,
  "description": "Guided movement routines — Morning Flow and Hip Reset — as an offline-capable web app.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@fontsource-variable/fraunces": "^5.2.5",
    "@fontsource-variable/instrument-sans": "^5.2.5",
    "@types/three": "^0.185.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
